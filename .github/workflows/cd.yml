name: 🚀 CD AI Agent

on:
  push:
    branches: ["main"]
    paths:
      - 'src/**'
  workflow_dispatch:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  deploy-platform:
    uses: ./.github/workflows/build-and-deploy-container-app-v2.yml
    with:
      componentName: "PLATFORM"
      serviceName: "ai-core"
      dockerfile: "Dockerfile"
      environments: |
        [
          { "name": "Development", "region": "westeurope" },
          { "name": "QA", "region": "westeurope" }
        ]
    secrets: inherit
